# Machine-local state generated by fish (colors, universal variables).
# ~/.config/fish is a symlink into this repo, so the file lands here even though
# it is machine-specific and rewritten constantly.
fish/.config/fish/fish_variables
